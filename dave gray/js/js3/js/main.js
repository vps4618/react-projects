// code challange
const myName = "sarathchandra";
const arrayOfLetters = myName.split("");
const number = Math.floor(Math.random() * myName.length);

console.log(arrayOfLetters[number]);

const myDetails = {
    name:"vishwa praveen",
    age:17    
}

console.log(myDetails.name);